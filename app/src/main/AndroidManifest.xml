<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.ua.eps.androidavanzado">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pComponents" />
        <activity
            android:name=".sSecundary"
            android:theme="@style/sStyle" />
        <activity
            android:name=".sPrincipal"
            android:theme="@style/sStyle" />
        <activity android:name=".Drawables" />
        <activity android:name=".Notifications_StateBar" />
        <activity android:name=".Selection_Dialog" />
        <activity android:name=".Notifications" />
        <activity android:name=".Notifications_Snackbar" />
        <activity android:name=".Notifications_Toast" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>